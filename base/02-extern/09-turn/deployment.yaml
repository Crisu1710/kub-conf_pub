apiVersion: apps/v1
kind: Deployment
metadata:
  name: turn-deploy
  namespace: turn
  labels:
    environment: extern
    app: turn
spec:
  replicas: 1
  selector:
    matchLabels:
      app: turn
  template:
    metadata:
      labels:
        app: turn
    spec:
      hostNetwork: true
      containers:
        - name: turn
          image: crisu1710/turn_server
          env:
            - name: TURN_USER
              value: "admin"
            - name: TURN_PASSWD
              value: ""
            - name: TURN_REALM
              value: ""